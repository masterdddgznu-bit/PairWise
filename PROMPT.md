请在当前 TypeScript 仓库中补全 `src/cluster.ts`，使 `npm test` 与 `npm run build` 全部通过。

目标：实现进程内的 Dynamo 风格多副本存储 `Cluster`。没有真实网络，节点由字符串 id 标识；失败用 `fail` / `recover` 模拟。

必须满足：
1. 只允许修改 `src/`；禁止修改 `tests/`。
2. 构造函数 `new Cluster(nodeIds, W, R)`。`nodeIds` 顺序固定且不重复。初始全部在线。`W` / `R` 为写/读仲裁数。
3. 每个副本上，一个 key 保存一组版本；每个版本是 `{ value, clock }`，`clock` 为向量时钟（`Record<nodeId, number>`，缺省分量视为 0）。时钟 A 支配 B，当且仅当所有分量 A≥B 且至少一处严格大于。若互不支配，则视为并发兄弟版本，必须同时保留。
4. `put(key, value, context = {})`：若当前在线节点数 < `W`，直接抛错，不改任何副本，也不写入暗示队列。否则协调者取「当前在线节点里、按 `nodeIds` 顺序的第一个」。新时钟 = 合并 `context` 与协调者本地该 key 已有全部版本时钟后，再把协调者自己的分量 +1。只向「当前在线节点里按 `nodeIds` 顺序的前 `W` 个」写入该版本；其余仍在线但未写入的节点保持原样（可能陈旧）；每个离线节点追加一条相同版本的暗示（FIFO）。返回 `{ coordinator, clock }`。
5. `get(key)`：按 `nodeIds` 顺序联系在线节点，凑满 `R` 个读确认；不足则抛错。汇总这些节点上该 key 的全部版本，丢掉被其他版本支配的旧版本，剩下的兄弟版本按 `value` 字典序排序后返回。返回的 `context` 为这些幸存版本时钟的逐分量最大值合并。然后做读修复：对本次实际读到的每个节点，若其本地该 key 的版本集合与幸存集合不完全相同（按 value+clock 精确比较），则用幸存集合整份覆盖该节点上的这个 key。
6. `fail(id)` 使节点离线；对已离线再 fail 可忽略。`recover(id)` 使节点重新上线，并按 FIFO 投递积压给它的暗示：每条暗示把版本合并进该节点本地（同 clock 不重复；被支配的旧版本删除；并发则并存）。暗示投递后清空该节点队列。对已在线再 recover 只要求不抛错。
7. `dump(id, key)` 返回该节点本地该 key 幸存版本的浅拷贝列表（已去掉被支配项），按 `value` 字典序排序；节点不存在时抛错。`stats()` 返回 `{ up, hints }`：当前在线节点数，以及所有离线节点暗示队列长度之和。
8. 不要引入外部依赖，不要改测试。

验收：`npm test` 全绿，`npm run build` 通过，且未改动 `tests/`。
