请阅读并修复当前 TypeScript 仓库中的 `src/`，使 `npm test` 与 `npm run build` 全部通过。

这是一个可运行的工作流引擎半成品。简单线性流程通常正常；当你把「依赖传播、重试、取消、回滚、缓存世代」组合在一起时，会出现不一致。请从数据流/状态流出发定位，而不是只改一处显而易见的分支。

只允许修改 `src/`；禁止修改 `tests/`；不要引入外部依赖。时间一律用逻辑 tick（`Engine.tick`），不要使用真实等待。

对外入口是 `Engine`（见 `src/engine.ts`）。核心约定（测试会组合验证，细节以测试行为为准）：
1. 工作流是步骤 DAG：`deps` 表示前驱。仅当全部前驱成功且「结果世代」匹配时，后继才可调度。
2. 步骤状态机至少覆盖：`pending → running → succeeded|failed`，以及取消、回滚相关状态；非法迁移必须拒绝。
3. 失败可按 `retry` 策略在未来 tick 重试；重试会提升该步骤的结果世代，并需要让依赖它的后继失效后重算。
4. `cancel(runId)` 后，未完成步骤不得再成功提交；已成功但处于取消影响范围的下游应进入补偿回滚，且回滚顺序必须是依赖拓扑的逆序。
5. 步骤输出可缓存；缓存键必须区分运行、步骤与世代。回滚或世代提升后，旧缓存不可再被当作成功输入。
6. 同一 `runId` 上的并发 `tick` 不应破坏状态（测试在单线程里交错调用）；`start` 同一工作流定义可多次，每次 run 隔离。

验收：`npm test` 全绿，`npm run build` 通过，且未改动 `tests/`。
