请在当前 TypeScript 仓库中补全 `src/store.ts`，使 `npm test` 与 `npm run build` 全部通过。

目标：实现进程内页存储 `Store`。每个 key 单独占一页。没有真实磁盘，但要区分内存缓冲和已刷出的页。恢复语义接近 ARIES：可以偷取脏页（未提交的写入允许先刷盘），提交不必强制刷盘，检查点是模糊的。

必须满足：
1. 只允许修改 `src/`；禁止修改 `tests/`。
2. `begin` 分配从 1 递增的事务号，不写日志。`put` / `del` 只作用于活跃事务：先记更新日志，再改缓冲页。日志里的更新记录要包含事务号、该事务上一条日志的 lsn（没有则为 0）、key、旧镜像、新镜像。缺失镜像用空值表示，对外 API 仍返回 `undefined`。
3. 同一时刻一个 key 只能被一个活跃事务修改。其他活跃事务再 `put` / `del` 这个 key 必须抛错，且不得追加日志，即使当前缓冲里的镜像已经是空的。只有没有任何活跃事务持有该 key、且当前镜像已缺失时，`del` 才不写日志、不加锁。`get(tx, key)` 能看见本事务尚未提交的写入；`read(key)` 只看见已提交版本。`commit` 只追加提交记录并公开本事务的结果，不要求刷盘。`abort` 按该事务的 lsn 链从新到旧撤销：每条更新追加一条补偿日志（CLR），把页装回那条更新的旧镜像，CLR 记下被补偿更新的上一条 lsn；链走完再追加 abort。撤销循环要沿着旧镜像链前进，不能停在最后一条，也不要把刚写下的 CLR 再当成待撤销记录。
4. `flush` 把全部脏页写成已刷出版本（含空值页，以便保留页 lsn），并清空脏标记。`checkpoint` 追加一条检查点，记下当时活跃事务的事务号和各自 lastLsn，以及每个脏页的 key 和 recLsn（该页本轮变脏时的第一条 lsn）。检查点本身不刷盘。
5. `crash` 丢掉缓冲、脏页表、事务表和已提交视图，只保留日志与已刷出的页。崩溃之后、`recover` 之前，`read` 必须全部返回 `undefined`，不能把磁盘页直接当成提交结果。除 `recover` 和 `stats` 外，崩溃后的其他操作应抛错。
6. `recover` 分三步。分析：从最后一条检查点恢复活跃事务和脏页表（没有检查点就从日志头开始），再正向扫描到日志末尾；看到提交或 abort 就移出活跃集；更新和 CLR 会把事务留在活跃集并刷新 lastLsn，同时补齐脏页的 recLsn。重做：从脏页表里最小的 recLsn 开始（它可能小于检查点的 lsn），按顺序重放更新和 CLR；若该页已刷出的 lsn 已经不小于记录 lsn，则跳过写入，但仍要能读到这页。撤销：分析结束时仍活跃的事务是失败者。对每个失败者从 lastLsn 沿链撤销，规则与 `abort` 相同（已有 CLR 则跳到它的 undoNext），最后补 abort。已经有 abort 记录的事务不能再次撤销。恢复结束后，`read` 以「缓冲覆盖已刷出页」为准，空值视为不存在。
7. 再次 `crash` + `recover` 必须得到同一提交结果，且不能重复追加 CLR 或 abort。
8. 不要引入外部依赖，不要改测试。

`stats()` 返回 `{ active, log, dirty, disk }`：活跃事务数、日志条数、尚未刷盘的脏页数、已刷出页数（含空值页）。

验收：`npm test` 全绿，`npm run build` 通过，且未改动 `tests/`。
