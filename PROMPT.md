请在当前 TypeScript 仓库中补全 `src/lock_manager.ts`，使 `npm test` 与 `npm run build` 全部通过。

目标：实现进程内多资源锁管理器 `LockManager`。

必须满足：
1. 只允许修改 `src/`；禁止修改 `tests/`。
2. 锁模式：`S`（共享）与 `X`（排他）。同一资源上多个 `S` 可并存；`X` 与任何其他持有者互斥。同一事务对同一资源重复申请已持有的兼容模式应直接成功（可重入），`S` 升级到 `X` 时若还有其他持有者则必须等待。
3. 等待队列按申请先后 FIFO。锁释放后只唤醒队首，且仅当队首与当前持有者兼容时授予；不兼容则继续等待，后面的请求不得插队。
4. 死锁：若本次等待会在等待图上形成环，必须立即中止等待图中 `txnId` 字典序最大的那个事务（`abort`），并释放它持有的全部锁、把它从所有队列移除，然后继续处理。被中止事务的 `status` 为 `aborted`，且后续 `lock` 应抛错。
5. 超时：`lock` 可带 `timeoutMs`。若在该时间内仍未获得锁（以 `clock` 为准，测试会 `advance` 后调用 `tick()`），该事务被中止，规则同死锁中止。
6. `commit` 与 `abort` 都释放该事务全部锁并唤醒后续兼容等待者。`crash(txnId)` 等价于持有者消失：释放其锁并唤醒，状态为 `aborted`。
7. 不要引入外部依赖，不要改测试。

验收：`npm test` 全绿，`npm run build` 通过，且未改动 `tests/`。
